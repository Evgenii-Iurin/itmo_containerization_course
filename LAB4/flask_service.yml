apiVersion: v1
kind: Service
metadata:
  name: flask-service
  labels:
    app: lab4
    component: app
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
    nodePort: 30080
  selector:
    app: lab4
    component: app